import { LayoutBase } from '@components';
import { HomeContainer } from '@containers';



const Home: React.FC = () => {
	return (
		<LayoutBase pageKey='' pageTitle=''>
			<HomeContainer />
		</LayoutBase>
	);
};

export default Home;

